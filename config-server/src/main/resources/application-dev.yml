server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/silviobassi/ead-config-server-repository
          default-label: main
          username: silviobassi
          password: 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
ead:
  configServer:
    username: configserver
    password: 'ead123cs'
  serviceRegistry:
    username: serviceregistry
    password: 'ead123sr'

eureka:
  client:
    service-url:
      defaultZone: 'http://${ead.serviceRegistry.username}:${ead.serviceRegistry.password}@localhost:8761/eureka'
  instance:
    hostname: localhost
